# Telegram Bot Token from @BotFather
BOT_TOKEN=your-bot-token-here

# Your Telegram user ID (integer). Messages from other users are silently ignored.
# Send /start to @userinfobot on Telegram to find yours.
AUTHORIZED_USER_ID=123456789

# Path to the message log file (optional, defaults to ./data/messages.log)
# MESSAGES_FILE=./data/messages.log

# Anthropic API key for Claude LLM integration
# Get yours at https://console.anthropic.com/
ANTHROPIC_API_KEY=your-api-key-here

# LLM model to use (optional, defaults to claude-sonnet-4-20250514)
# LLM_MODEL=claude-sonnet-4-20250514

# SQLite database path (optional, defaults to ./data/knowledge.db)
# DB_PATH=./data/knowledge.db

# Conversation log database path (optional, defaults to ./data/conversations.db)
# Stores full LLM interaction history (system prompts, responses, etc.)
# CONVERSATION_LOG_DB_PATH=./data/conversations.db

# Overview markdown export path (optional, defaults to ./data/overview.md)
# Auto-updated every time the rolling overview changes
# OVERVIEW_MD_PATH=./data/overview.md
